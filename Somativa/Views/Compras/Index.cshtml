@model IEnumerable<Somativa.Models.Compra>

@{
    ViewData["Title"] = "Index";
}

<h1>Compras</h1>

<p>
    <a asp-action="Create" class="btn btn-warning m-2">
        <svg bootstrap-icon="Plus" width="16" height="16" aria-label="Create"></svg>
        Criar
    </a>
</p>

<table class="table table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nota)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataHora)
            </th>
            <th>

            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nota)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataHora)
            </td>
                <td class="text-end">
                    <a asp-action="Edit" class="btn m-2 text-white" style="background-color:lightseagreen" asp-route-id="@item.CompraId">
                        <svg bootstrap-icon="Pencil" width="16" height="16" aria-label="Editar"></svg>
                        Editar
                    </a>
                    <a asp-action="Details" class="btn m-2 text-white" style="background-color:cadetblue" asp-route-id="@item.CompraId">
                        <svg bootstrap-icon="InfoCircle" width="16" height="16" aria-label="Mais"></svg>
                        Detalhar
                    </a>
                    <a asp-action="Delete" class="btn m-2 text-white" style="background-color:darkslategrey" asp-route-id="@item.CompraId">
                        <svg bootstrap-icon="Trash2" width="16" height="16" aria-label="Deletar"></svg>
                        Deletar
                    </a>
                    <a asp-controller="CompraItems" asp-action="Index" class="btn m-2 text-white" style="background-color:lightblue" asp-route-id="@item.CompraId">
                        <svg bootstrap-icon="Trash2" width="16" height="16" aria-label="Items"></svg>
                        Itens
                    </a>
            </td>
        </tr>
}
    </tbody>
</table>
